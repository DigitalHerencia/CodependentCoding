# LV-110 — Package, docs, and first generator release polish

## Outcome

Make the completed generator easy to install, understand, and use as a real product.

## Scope

- finalize package/bin naming and compatibility transition;
- ensure packaged CLI contains the golden template/modules/schema it needs;
- refresh README with CLI, web configurator, presets, commands, and generated-app expectations;
- document provider setup handoff and limitations;
- perform only the release-oriented smoke checks needed to establish that the published-style package can create a representative app.

## Acceptance

- documented install/create command matches the packaged executable;
- package contains required runtime assets;
- representative packed-CLI generation works for the release path;
- docs accurately distinguish what Loaded Vibes creates from what the user/provider must configure.
